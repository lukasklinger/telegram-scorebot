version: '2'

services:
  scorebot:
    build: .
    restart: unless-stopped
    volumes:
      - scorebot-data:/usr/src/app/data
    environment:
      - BOT_TOKEN=5161567991:AAGa6kyNURytAjAN7bLzcGyR_bUU_LqVHNM

volumes:
  scorebot-data:
